<UserControl x:Class="ELM.WPF.UI.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:services="clr-namespace:ELM.WPF.Services"
             xmlns:local="clr-namespace:ELM.WPF.UI"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Width="300" Margin="20">
        <TextBlock Text="Username" Margin="0 0 0 5"/>
        <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"/>

        <TextBlock Text="Password" Margin="0 10 0 5"/>
        <PasswordBox x:Name="PasswordBox" 
             services:PasswordBoxHelper.Password="{Binding Password, Mode=TwoWay}"/>

        <Button Content="Login" Command="{Binding LoginCommand}" Margin="0 20"/>
    </StackPanel>
</UserControl>
